<?php if (!defined('APPLICATION')) exit();
abstract class {{Index}}API extends {{Index}}Utility {


  /*
   * @@ GDN Plugin Scaffold Suggestion @@
   *
   * Include any methods, ans properties
   * specifc to the plugin
   *
   * e.g.
   *
   * protected $SomeProperties = array();
   *
   * public function Init(){
   *
   * 		$this->SomeProperties['Key'] = 'Value';
   * 		$this->FireEvent('Load{{Index}}');
   * }
   *
   * public function SetProperty($Key, $Value){
   * 		$this->SomeProperties[$Key] = $Value;
   * }
   *
   * public function GetProperty($Key, $Value){
   * 		return GetValue($Key, $this->SomeProperties, NULL);
   * }
   *
   * public function Cron(){
   * 		// cron specific stuff
   * }
   *
   * // helpers
   *
   * public static function GenerateToken(){
   * 		return '{{index}}_'.unique();
   * }
   *
   * public function PrintProperties(){
   * 		return var_export($this->SomeProperties, TRUE);
   * }
   *
   */



}